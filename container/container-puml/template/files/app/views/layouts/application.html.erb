<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "service-puml" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag :app %>
    <style>
      :root {
        --bg: #fefefe;
        --bg-secondary: #eef2ff;
        --border: #c7d2fe;
        --text: #1e1b4b;
        --text-muted: #6366f1;
        --link: #4f46e5;
        --accent: #6366f1;
        --accent-light: #a5b4fc;
      }
      * { box-sizing: border-box; margin: 0; padding: 0; }
      body {
        font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Helvetica, Arial, sans-serif;
        background: var(--bg);
        color: var(--text);
        line-height: 1.5;
      }
      a { color: var(--link); text-decoration: none; }
      a:hover { text-decoration: underline; }
      .container { max-width: 960px; margin: 0 auto; padding: 24px 16px; }
      header {
        background: var(--bg-secondary);
        border-bottom: 2px solid var(--border);
        padding: 12px 16px;
      }
      header .inner { max-width: 960px; margin: 0 auto; display: flex; align-items: center; gap: 16px; }
      header a.brand { color: var(--accent); font-weight: 600; font-size: 18px; }
      .diagram-card {
        background: #fff;
        border: 1px solid var(--border);
        border-radius: 8px;
        padding: 24px;
        margin-top: 24px;
        box-shadow: 0 1px 3px rgba(99, 102, 241, 0.1);
      }
      .diagram-card h2 {
        color: var(--accent);
        margin-bottom: 16px;
        font-size: 20px;
      }
      .diagram-card svg {
        max-width: 100%;
        height: auto;
      }
      .service-puml-error {
        background: #fef2f2;
        border: 1px solid #fca5a5;
        border-radius: 6px;
        padding: 12px 16px;
        color: #991b1b;
        font-size: 14px;
      }
      .flash { padding: 12px 16px; border-radius: 6px; margin-bottom: 16px; font-size: 14px; }
      .flash-notice { background: var(--bg-secondary); border: 1px solid var(--border); }
      .flash-alert { background: #fef2f2; border: 1px solid #fca5a5; color: #991b1b; }
      .page-header { display: flex; align-items: center; justify-content: space-between; margin-bottom: 16px; }
      .page-header h1 { font-size: 24px; color: var(--text); }
    </style>
  </head>

  <body>
    <header>
      <div class="inner">
        <a href="/" class="brand">service-puml</a>
      </div>
    </header>

    <div class="container">
      <% if flash[:notice] %>
        <div class="flash flash-notice"><%= flash[:notice] %></div>
      <% end %>
      <% if flash[:alert] %>
        <div class="flash flash-alert"><%= flash[:alert] %></div>
      <% end %>

      <%= yield %>
    </div>
  </body>
</html>
