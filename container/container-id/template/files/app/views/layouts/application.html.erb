<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "service-id" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag :app %>
    <style>
      :root {
        --bg: #0d1117;
        --bg-secondary: #161b22;
        --border: #30363d;
        --text: #e6edf3;
        --text-muted: #8b949e;
        --link: #58a6ff;
        --btn-bg: #238636;
        --btn-hover: #2ea043;
        --danger: #da3633;
        --verified: #3fb950;
      }
      * { box-sizing: border-box; margin: 0; padding: 0; }
      body {
        font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Helvetica, Arial, sans-serif;
        background: var(--bg);
        color: var(--text);
        line-height: 1.5;
      }
      a { color: var(--link); text-decoration: none; }
      a:hover { text-decoration: underline; }
      .container { max-width: 960px; margin: 0 auto; padding: 24px 16px; }
      header {
        background: var(--bg-secondary);
        border-bottom: 1px solid var(--border);
        padding: 12px 16px;
      }
      header .inner { max-width: 960px; margin: 0 auto; display: flex; align-items: center; gap: 16px; }
      header a.brand { color: var(--text); font-weight: 600; font-size: 18px; }
      .btn {
        display: inline-block;
        padding: 6px 16px;
        border-radius: 6px;
        border: 1px solid var(--border);
        background: var(--btn-bg);
        color: #fff;
        font-size: 14px;
        cursor: pointer;
        font-weight: 500;
      }
      .btn:hover { background: var(--btn-hover); text-decoration: none; }
      .btn-danger { background: var(--danger); border-color: var(--danger); }
      .btn-danger:hover { background: #b62324; }
      .btn-outline { background: transparent; color: var(--text); }
      .btn-outline:hover { background: var(--border); }
      .identity-list { list-style: none; }
      .identity-list li {
        padding: 16px 0;
        border-bottom: 1px solid var(--border);
        display: flex;
        align-items: center;
        gap: 12px;
      }
      .identity-avatar {
        width: 40px;
        height: 40px;
        border-radius: 50%;
        background: var(--border);
        flex-shrink: 0;
      }
      .identity-info { flex: 1; }
      .identity-handle { font-size: 18px; font-weight: 600; }
      .identity-name { color: var(--text-muted); font-size: 14px; }
      .badge-verified {
        display: inline-block;
        padding: 2px 8px;
        border-radius: 12px;
        font-size: 12px;
        font-weight: 500;
        background: rgba(63, 185, 80, 0.15);
        color: var(--verified);
        border: 1px solid rgba(63, 185, 80, 0.3);
      }
      .profile-header { display: flex; align-items: flex-start; gap: 20px; margin-bottom: 24px; }
      .profile-avatar {
        width: 80px;
        height: 80px;
        border-radius: 50%;
        background: var(--border);
        flex-shrink: 0;
      }
      .profile-details { flex: 1; }
      .profile-handle { font-size: 24px; font-weight: 600; }
      .profile-name { color: var(--text-muted); font-size: 16px; margin-top: 2px; }
      .profile-bio { margin-top: 8px; color: var(--text); }
      .link-list { list-style: none; margin-top: 16px; }
      .link-list li {
        padding: 10px 16px;
        border: 1px solid var(--border);
        border-radius: 6px;
        margin-bottom: 8px;
        font-size: 14px;
      }
      .link-rel { color: var(--text-muted); font-size: 12px; font-family: monospace; }
      .link-title { font-weight: 500; }
      .form-group { margin-bottom: 16px; }
      .form-group label { display: block; margin-bottom: 4px; font-size: 14px; font-weight: 500; }
      .form-group input, .form-group textarea {
        width: 100%;
        padding: 8px 12px;
        border: 1px solid var(--border);
        border-radius: 6px;
        background: var(--bg);
        color: var(--text);
        font-size: 14px;
      }
      .form-group textarea { min-height: 80px; resize: vertical; }
      .flash { padding: 12px 16px; border-radius: 6px; margin-bottom: 16px; font-size: 14px; }
      .flash-notice { background: #1a3a2a; border: 1px solid #238636; }
      .flash-alert { background: #3a1a1a; border: 1px solid var(--danger); }
      .empty-state { text-align: center; padding: 48px 0; color: var(--text-muted); }
      .page-header { display: flex; align-items: center; justify-content: space-between; margin-bottom: 16px; }
      .page-header h1 { font-size: 24px; }
      form.inline { display: inline; }
    </style>
  </head>

  <body>
    <header>
      <div class="inner">
        <a href="/" class="brand">service-id</a>
      </div>
    </header>

    <div class="container">
      <% if flash[:notice] %>
        <div class="flash flash-notice"><%= flash[:notice] %></div>
      <% end %>
      <% if flash[:alert] %>
        <div class="flash flash-alert"><%= flash[:alert] %></div>
      <% end %>

      <%= yield %>
    </div>
  </body>
</html>
