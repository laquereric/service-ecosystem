<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "service-github" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag :app %>
    <style>
      :root {
        --bg: #0d1117;
        --bg-secondary: #161b22;
        --border: #30363d;
        --text: #e6edf3;
        --text-muted: #8b949e;
        --link: #58a6ff;
        --btn-bg: #238636;
        --btn-hover: #2ea043;
        --danger: #da3633;
      }
      * { box-sizing: border-box; margin: 0; padding: 0; }
      body {
        font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Helvetica, Arial, sans-serif;
        background: var(--bg);
        color: var(--text);
        line-height: 1.5;
      }
      a { color: var(--link); text-decoration: none; }
      a:hover { text-decoration: underline; }
      .container { max-width: 960px; margin: 0 auto; padding: 24px 16px; }
      header {
        background: var(--bg-secondary);
        border-bottom: 1px solid var(--border);
        padding: 12px 16px;
      }
      header .inner { max-width: 960px; margin: 0 auto; display: flex; align-items: center; gap: 16px; }
      header a.brand { color: var(--text); font-weight: 600; font-size: 18px; }
      .btn {
        display: inline-block;
        padding: 6px 16px;
        border-radius: 6px;
        border: 1px solid var(--border);
        background: var(--btn-bg);
        color: #fff;
        font-size: 14px;
        cursor: pointer;
        font-weight: 500;
      }
      .btn:hover { background: var(--btn-hover); text-decoration: none; }
      .btn-danger { background: var(--danger); border-color: var(--danger); }
      .btn-danger:hover { background: #b62324; }
      .btn-outline { background: transparent; color: var(--text); }
      .btn-outline:hover { background: var(--border); }
      .repo-list { list-style: none; }
      .repo-list li {
        padding: 16px 0;
        border-bottom: 1px solid var(--border);
      }
      .repo-list .repo-name { font-size: 20px; font-weight: 600; }
      .repo-list .repo-desc { color: var(--text-muted); font-size: 14px; margin-top: 4px; }
      .file-tree { list-style: none; border: 1px solid var(--border); border-radius: 6px; overflow: hidden; }
      .file-tree li {
        padding: 8px 16px;
        border-bottom: 1px solid var(--border);
        font-size: 14px;
        font-family: monospace;
      }
      .file-tree li:last-child { border-bottom: none; }
      .file-tree .icon { margin-right: 8px; }
      .code-block {
        background: var(--bg-secondary);
        border: 1px solid var(--border);
        border-radius: 6px;
        padding: 16px;
        overflow-x: auto;
        font-family: monospace;
        font-size: 13px;
        line-height: 1.6;
        white-space: pre;
      }
      .commit-list { list-style: none; }
      .commit-list li {
        padding: 12px 0;
        border-bottom: 1px solid var(--border);
      }
      .commit-msg { font-weight: 500; }
      .commit-meta { color: var(--text-muted); font-size: 13px; margin-top: 2px; }
      .commit-sha { font-family: monospace; font-size: 12px; color: var(--link); }
      .breadcrumb { font-size: 14px; color: var(--text-muted); margin-bottom: 16px; }
      .breadcrumb a { color: var(--link); }
      form.inline { display: inline; }
      .form-group { margin-bottom: 16px; }
      .form-group label { display: block; margin-bottom: 4px; font-size: 14px; font-weight: 500; }
      .form-group input, .form-group textarea {
        width: 100%;
        padding: 8px 12px;
        border: 1px solid var(--border);
        border-radius: 6px;
        background: var(--bg);
        color: var(--text);
        font-size: 14px;
      }
      .form-group textarea { min-height: 80px; resize: vertical; }
      .flash { padding: 12px 16px; border-radius: 6px; margin-bottom: 16px; font-size: 14px; }
      .flash-notice { background: #1a3a2a; border: 1px solid #238636; }
      .flash-alert { background: #3a1a1a; border: 1px solid var(--danger); }
      .empty-state { text-align: center; padding: 48px 0; color: var(--text-muted); }
      .page-header { display: flex; align-items: center; justify-content: space-between; margin-bottom: 16px; }
      .page-header h1 { font-size: 24px; }
      .clone-url {
        background: var(--bg-secondary);
        border: 1px solid var(--border);
        border-radius: 6px;
        padding: 8px 12px;
        font-family: monospace;
        font-size: 13px;
        color: var(--text-muted);
        margin-bottom: 16px;
      }
      .tab-nav { display: flex; gap: 0; border-bottom: 1px solid var(--border); margin-bottom: 16px; }
      .tab-nav a {
        padding: 8px 16px;
        color: var(--text-muted);
        border-bottom: 2px solid transparent;
        font-size: 14px;
      }
      .tab-nav a.active { color: var(--text); border-bottom-color: #f78166; }
    </style>
  </head>

  <body>
    <header>
      <div class="inner">
        <a href="/" class="brand">service-github</a>
      </div>
    </header>

    <div class="container">
      <% if flash[:notice] %>
        <div class="flash flash-notice"><%= flash[:notice] %></div>
      <% end %>
      <% if flash[:alert] %>
        <div class="flash flash-alert"><%= flash[:alert] %></div>
      <% end %>

      <%= yield %>
    </div>
  </body>
</html>
